import { describe, test } from "vitest";

describe('isPalindrome', () => {
    test('should recognize "aabbaa" as a palindrome', () => {

    });

    test('should not recognize "vite-test" ', () => {
        
    });

    test('should not recognize  empty string like palindrome ', () => {
        
    });
  });

